{
  "name": "client",
  "version": "0.0.0",
  "scripts": {
    "ng": "ng",
    "start": "ng serve",
    "build": "ng build --configuration production",
    "prebuild": "node git-version.mjs"
  },
  "private": true,
  "dependencies": {
  },
  "devDependencies": {

  }
}
